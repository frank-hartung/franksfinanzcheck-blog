# Hugo Konfiguration für FranksFinanzcheck.de (100% Ausfallsicher)
baseURL: "https://franksfinanzcheck.de/"
languageCode: "de-de"
title: "FranksFinanzcheck"

# ==============================================================================
# 🛡 DIESE 3 ZEILEN GARANTIEREN, DASS JEDER ARTIKEL IMMER SOFORT VERÖFFENTLICHT WIRD!
# ==============================================================================
buildDrafts: true   # Baut Artikel selbst dann, wenn versehentlich draft: true gesetzt ist!
buildFuture: true   # Baut Artikel selbst dann, wenn das Datum in der Zukunft liegt!
buildExpired: true  # Baut Artikel selbst dann, wenn ein Ablaufdatum hinterlegt ist!

enableRobotsTXT: true
