diff --git a/.github/workflows/blog-health-daily.yml b/.github/workflows/blog-health-daily.yml
index c0ce374..56dc07b 100644
--- a/.github/workflows/blog-health-daily.yml
+++ b/.github/workflows/blog-health-daily.yml
@@ -38,6 +38,13 @@ jobs:
       - run: python3 scripts/draft_link_healer.py --fix
 
       - run: python3 scripts/blog_health_gate.py
+      # Premium-Zeichenlänge (Google + Pinterest Landing): Report + KI-Heilung
+      # der kürzesten Unterlänge-Seiten (Floor Posts 10.000 / Pillar 12.000).
+      - name: Premium-Zeichenlänge
+        env:
+          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
+          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
+        run: python3 scripts/length_guard.py --fix --ai --backlog 3 || echo "⚠ Length-Guard – Funde in LENGTH-REPORT.md"
       - run: |
           git config user.name "Agent (Arena.ai)"
           git config user.email "agent@franksfinanzcheck.de"
diff --git a/.github/workflows/content-engine-v2.yml b/.github/workflows/content-engine-v2.yml
index b02ed22..0c6d052 100644
--- a/.github/workflows/content-engine-v2.yml
+++ b/.github/workflows/content-engine-v2.yml
@@ -155,8 +155,8 @@ jobs:
           python3 scripts/fix_dash_und.py --fix || echo "⚠ Gedankenstrich-Check (nicht kritisch)"
           echo "== Gedankenstrich-am-Zeilenende (deterministisch, selbstheilend) =="
           python3 scripts/fix_dash_eol.py --fix || echo "⚠ Gedankenstrich-EOL-Check (nicht kritisch)"
-          echo "== Zeichenlängen-Gate (Hoheit: posts | Affiliate-Floor 1000 via LENGTH_MIN_WORDS) =="
-          LENGTH_MIN_WORDS="${{ vars.LENGTH_MIN_WORDS || '1000' }}" python3 scripts/check_length.py --fix || echo "⚠ Längen-Check (nicht kritisch)"
+          echo "== Zeichenlängen-Gate (Premium: Posts Floor 10.000 / Optimum 12.000–18.000 Zeichen) =="
+          python3 scripts/check_length.py --fix || echo "⚠ Längen-Check (nicht kritisch)"
           echo "== Leerzeichen-Generator (Profi-Level, selbstheilend) =="
           python3 scripts/fix_spaces.py || echo "⚠ Leerzeichen-Generator (nicht kritisch)"
           echo "== Rechtschreibung =="
@@ -180,8 +180,8 @@ jobs:
           python3 scripts/lektor_guard.py --fix --ai --new-only || echo "⚠ Lektorat (nicht kritisch)"
           echo "== Brand-Guard (Marken-Lock: Willkommenstext/Emojis/Disclaimer unantastbar) =="
           python3 scripts/brand_guard.py --fix || echo "⚠ Brand-Guard (nicht kritisch)"
-          echo "== Laengen-Guard PILLAR-SCOPE (Silo-Seiten >= 2.500 Woerter) =="
-          python3 scripts/length_guard.py --fix --ai --new-only --scope pillar || echo "⚠ Length-Guard Pillar (nicht kritisch)"
+          echo "== Laengen-Guard Premium (Posts + Pillar, SSOT length_policy.py) =="
+          python3 scripts/length_guard.py --fix --ai --new-only --scope all || echo "⚠ Length-Guard (nicht kritisch)"
           echo "== Profi-Text-Gate =="
           python3 scripts/profi_text_check.py --new-only || echo "⚠ Profi-Gate (nicht blockierend)"
           echo "== Lesbarkeit =="
diff --git a/.github/workflows/seo-weekly.yml b/.github/workflows/seo-weekly.yml
index 654952f..9edd1b4 100644
--- a/.github/workflows/seo-weekly.yml
+++ b/.github/workflows/seo-weekly.yml
@@ -45,6 +45,15 @@ jobs:
         # BUGFIX 13.08. / 26.08.: pyyaml, cryptography, pillow, fonttools, brotli
         run: python3 -m pip install -q pyyaml cryptography pillow pillow-avif-plugin fonttools brotli 2>/dev/null || true
 
+      - name: Premium-Zeichenlänge (Google + Pinterest Landing)
+        continue-on-error: true
+        env:
+          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
+          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
+        run: |
+          python3 scripts/length_guard.py --fix --ai --backlog 5 || echo "⚠ Length-Guard – Funde in LENGTH-REPORT.md"
+          python3 scripts/check_length.py || echo "⚠ check_length – Unterlänge gemeldet"
+
       - name: SEO-Audit ausführen
         id: audit
         continue-on-error: true
