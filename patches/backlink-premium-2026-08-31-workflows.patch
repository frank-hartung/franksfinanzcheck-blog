--- a/.github/workflows/backlink-weekly.yml
+++ b/.github/workflows/backlink-weekly.yml
@@ -1,18 +1,18 @@
 # ============================================================
-#  BACKLINK-AUTOMATION (wöchentlich, Montag 08:00 MESZ)
+#  BACKLINK-AUTOMATION PREMIUM (wöchentlich, Montag 11:00 MESZ)
 #
-#  Der Scout prüft die kuratierten Backlink-Opportunities
-#  (data/backlink_prospects.yaml), priorisiert sie nach Passung
-#  und generiert personalisierte Outreach-Texte in
-#  BACKLINK-REPORT.md. Das ABSENDEN bleibt bewusst manuell
-#  (Qualitäts-Gate – automatisches Posten wäre Spam).
+#  Scout (scripts/backlink_automation.py):
+#    – kuratierte Opportunities (data/backlink_prospects.yaml)
+#    – CRM in data/backlink_state.json (YAML wird NICHT dump’t)
+#    – Score Passung × Autorität × Saison / Aufwand
+#    – typspezifische Outreach-Pakete + Wochenpack (Kapazität 5)
+#  Das ABSENDEN bleibt bewusst manuell (Qualitäts-Gate).
+#  Automatisches Posten wäre Spam und riskiert Google-Strafen.
 #
-#  Der Report wird automatisch committet, damit du ihn immer
-#  aktuell im Repo findest (oder per E-Mail-Benachrichtigung).
+#  Selftest läuft VOR dem Scout (Exit 2 = Sabotage-Stop).
 #
-#  Zeitzonen: GitHub-Cron läuft UTC. MESZ = UTC+2, MEZ = UTC+1.
-#    Sommerzeit: 06:00 UTC = 08:00 MESZ
-#    Winterzeit: 07:00 UTC = 08:00 MEZ
+#  Zeitzonen: GitHub-Cron = UTC. MESZ = UTC+2, MEZ = UTC+1.
+#    Sommerzeit: 09:00 UTC = 11:00 MESZ
 # ============================================================
 name: Backlink-Automation
 
@@ -31,24 +31,37 @@
 jobs:
   backlink-scout:
     runs-on: ubuntu-latest
+    timeout-minutes: 15
     steps:
       - name: Repository auschecken
         uses: actions/checkout@v4
+        with:
+          fetch-depth: 0
 
-      - name: Python + Abhängigkeiten
-        run: pip install -q pyyaml
+      - name: Python 3.11 einrichten
+        uses: actions/setup-python@v5
+        with:
+          python-version: "3.11"
+
+      - name: Python-Abhängigkeiten robust installieren
+        run: |
+          python -m pip install --disable-pip-version-check \
+            --retries 5 --timeout 30 pyyaml
+
+      - name: Selftest (Sabotage-Schutz)
+        run: python3 scripts/backlink_automation.py --selftest
 
       - name: Backlink-Scout ausführen
         run: python3 scripts/backlink_automation.py
 
-      - name: Report + Status committen
+      - name: Report + CRM committen
         run: |
           git config user.name "Backlink-Bot"
           git config user.email "backlink-bot@users.noreply.github.com"
-          git add BACKLINK-REPORT.md data/backlink_prospects.yaml
+          git add BACKLINK-REPORT.md data/backlink_state.json
           if git diff --cached --quiet; then
             echo "Keine Änderungen – Report ist aktuell."
           else
-            git commit -m "backlinks: wöchentlicher Scout-Report generiert"
+            git commit -m "backlinks: wöchentlicher Premium-Scout-Report"
             scripts/git_sync.sh --push-only
           fi
