diff --git a/.github/workflows/seo-weekly.yml b/.github/workflows/seo-weekly.yml
index 6d6c9a2..fb83b8f 100644
--- a/.github/workflows/seo-weekly.yml
+++ b/.github/workflows/seo-weekly.yml
@@ -261,12 +261,34 @@ jobs:
         # generiert statt zu flicken) und LÖSCHT NIE einen bereits
         # veröffentlichten Artikel. Exit 1 nur, wenn nach dem
         # Heilungsversuch etwas übrig bleibt, das echte Textarbeit braucht.
+        # PREMIUM (02.09.2026, Issue #149): Detektor-Selbsttest VOR dem
+        # Bestands-Check (fail-closed), Maschinen-Zustand .bestand_gate_state.json
+        # und Exit-Code als Step-Output für die idempotente Issue-Pflege.
         id: bestand
         continue-on-error: true
         run: |
+          set +e
           hugo --minify > /dev/null 2>&1
+          # Wache muss erst SEHEN können, bevor sie urteilt – sonst wiederholt
+          # sich die "24/24 Phantom-Render-Funde"-Fehlerklasse (#149/#146).
+          python3 scripts/affiliate_integrity_gate.py --selftest > /tmp/integrity_selftest.log 2>&1
+          selftest_code=$?
+          if [ "$selftest_code" -ne 0 ]; then
+            echo "exit_code=2" >> "$GITHUB_OUTPUT"
+            echo "🟠 Detektor-Selbsttest fehlgeschlagen – Bestand-Gate fail-closed (nichts geheilt/verworfen)."
+            cat /tmp/integrity_selftest.log
+            exit 2
+          fi
           python3 scripts/bestand_gate.py | tee /tmp/bestand_log.txt
-          exit ${PIPESTATUS[0]}
+          code=${PIPESTATUS[0]}
+          echo "exit_code=${code}" >> "$GITHUB_OUTPUT"
+          case "$code" in
+            0) echo "✅ Bestand-Gate grün – alle bestehenden Artikel konform." ;;
+            1) echo "🔴 Bestand-Gate: nach Heilungsversuch bleiben Funde offen." ;;
+            2) echo "🟠 Bestand-Gate: Auswertungsfehler (fail-closed)." ;;
+            *) echo "⚠ Unerwarteter Bestand-Gate-Exit-Code ${code}" ;;
+          esac
+          exit "$code"
 
       - name: Bestand-Gate-Fixes + Report committen
         run: |
@@ -278,6 +300,7 @@ jobs:
           # add-Aufruf statt eines gemeinsamen (siehe uniqueness-guard.yml).
           git add content/ 2>/dev/null || true
           git add BESTAND-REPORT.md 2>/dev/null || true
+          git add .bestand_gate_state.json 2>/dev/null || true
           git add AFFILIATE-REPORT.md 2>/dev/null || true
           git add AFFILIATE-INTEGRITY-REPORT.md 2>/dev/null || true
           git add .affiliate_report.json 2>/dev/null || true
@@ -289,24 +312,157 @@ jobs:
             scripts/git_sync.sh --push-only
           fi
 
-      - name: Issue bei nicht automatisch heilbaren Bestandsartikeln
-        # Greift NUR, wenn nach der Heilung noch etwas übrig ist (Exit 1).
-        # Betroffene Artikel werden NIE gelöscht (siehe bestand_gate.py) –
-        # nur gemeldet, damit ein Mensch echte Textarbeit leisten kann.
-        if: steps.bestand.outcome == 'failure'
+      - name: Bestand-Gate-Issue pflegen (öffnen/aktualisieren bei Rot, schließen bei Grün)
+        # PREMIUM (02.09.2026, Issue #149): Die alte Logik hat bei jedem
+        # wöchentlichem Rotlauf ein NEUES Issue erzeugt und es bei Grün nie
+        # geschlossen – deshalb blieb #149 offen, obwohl der Bestand längst
+        # grün ist. Jetzt idempotent: EIN offenes Issue pro Schadenslage,
+        # bei Grün automatisch schließen, Duplikate aufräumen. Auch
+        # Alt-Issues ohne Marker (#149) werden erkannt und geschlossen.
+        if: always()
+        env:
+          GATE_EXIT: ${{ steps.bestand.outputs.exit_code }}
         uses: actions/github-script@v7
         with:
           script: |
             const fs = require('fs');
-            let details = 'Bestand-Gate hat Artikel gefunden, die die aktuellen Publish-Gate-Regeln nicht (mehr) erfüllen.';
-            try { details = fs.readFileSync('BESTAND-REPORT.md', 'utf-8'); } catch (e) {}
-            await github.rest.issues.create({
-              owner: context.repo.owner,
-              repo: context.repo.repo,
-              title: '📋 Bestand-Gate: bestehende Artikel brauchen Aufmerksamkeit',
-              body: details + '\n\n---\nAutomatisch erstellt vom wöchentlichen SEO-Workflow (Bestand-Gate). Betroffene Artikel wurden NICHT gelöscht.'
-            });
-            console.log('✅ Issue erstellt');
+            const marker = '<!-- bestand-gate -->';
+            const exitCode = String(process.env.GATE_EXIT || '');
+            const green = exitCode === '0';
+            const toolError = exitCode === '2' || exitCode === '';
+
+            let report = '';
+            try { report = fs.readFileSync('BESTAND-REPORT.md', 'utf-8'); }
+            catch (e) { report = '_Report konnte nicht gelesen werden._'; }
+            let log = '';
+            try { log = fs.readFileSync('/tmp/bestand_log.txt', 'utf-8'); }
+            catch (e) { log = fs.readFileSync('/tmp/integrity_selftest.log', 'utf-8'); }
+
+            // Alt- und Neu-Issues zusammenfinden: Marker im Body (neu) oder
+            // exakter Legacy-Titel (#149) – immer ohne PullRequests.
+            const listed = await github.rest.issues.listForRepo({
+              owner: context.repo.owner, repo: context.repo.repo,
+              state: 'open', per_page: 100,
+            }).catch(() => ({ data: [] }));
+            const existing = (listed.data || []).filter(i => !i.pull_request &&
+              ((i.body || '').includes(marker) ||
+               i.title === '📋 Bestand-Gate: bestehende Artikel brauchen Aufmerksamkeit'));
+
+            // Alt-Issues bekommen das Auto-Report-Label, damit der Aufräum-
+            // Workflow sie später dauerhaft löschen darf.
+            if (existing.length) {
+              try {
+                await github.rest.issues.createLabel({
+                  owner: context.repo.owner, repo: context.repo.repo,
+                  name: 'auto-report', color: 'd73a4a',
+                  description: 'Automatisch erkannte Fehlermeldung'
+                });
+              } catch (e) { /* Label existiert bereits – ok */ }
+              for (const issue of existing) {
+                const hasLabel = Array.isArray(issue.labels) && issue.labels.some(l => l.name === 'auto-report');
+                if (!hasLabel) {
+                  try {
+                    await github.rest.issues.addLabels({
+                      owner: context.repo.owner, repo: context.repo.repo,
+                      issue_number: issue.number, labels: ['auto-report'],
+                    });
+                  } catch (e) { /* Label-Setzen optional */ }
+                }
+              }
+            }
+
+            if (green) {
+              for (const issue of existing) {
+                await github.rest.issues.createComment({
+                  owner: context.repo.owner, repo: context.repo.repo,
+                  issue_number: issue.number,
+                  body: `${marker}\n✅ **Bestand-Gate grün** (${new Date().toISOString()}): ` +
+                        `alle bestehenden Artikel erfüllen die aktuelle Publish-Gate-Prüfung ` +
+                        `(Länge, SEO, Affiliate-Profi, Affiliate-Integrität).\n\nIssue wird automatisch geschlossen.`,
+                });
+                await github.rest.issues.update({
+                  owner: context.repo.owner, repo: context.repo.repo,
+                  issue_number: issue.number, state: 'closed', state_reason: 'completed',
+                });
+                console.log(`✅ Issue #${issue.number} automatisch geschlossen (Bestand-Gate grün).`);
+              }
+              if (!existing.length) console.log('✅ Bestand-Gate grün, kein offenes Issue vorhanden.');
+              return;
+            }
+
+            const title = toolError
+              ? '📋 Bestand-Gate: Auswertung nicht möglich (Werkzeugfehler, fail-closed)'
+              : '📋 Bestand-Gate: bestehende Artikel brauchen Aufmerksamkeit';
+            const body = [
+              marker,
+              toolError
+                ? '> 🟠 **Werkzeugfehler (fail-closed):** Der Render-/Selbst-Beweis konnte nicht geführt werden ' +
+                  '(Hugo-Build, `public/` oder Detektor). Es wurde **nichts geheilt/verworfen**. ' +
+                  'Bitte `python3 scripts/affiliate_integrity_gate.py --selftest` laufen lassen – schlägt der ' +
+                  'Selbsttest fehl, muss der Detektor an `layouts/_default/_markup/render-link.html` angepasst ' +
+                  'werden (Drift-Wächter).'
+                : '> 🔴 **Bestand auffällig:** Nach der automatischen Selbstheilung bleiben Bestandsartikel ' +
+                  'auffällig. Betroffene Artikel wurden NICHT gelöscht – es braucht echte Text-/Content-Arbeit.',
+              '',
+              `**Lauf:** ${new Date().toISOString()} · **Exit-Code:** ${exitCode || 'unbekannt'} · ` +
+              `**Geprüfte Artikel:** ${report.match(/Geprüfte Live-Artikel:\s*(\d+)/)?.[1] || '?'} · ` +
+              `**Weiterhin auffällig:** ${report.match(/Weiterhin auffällig:\s*(\d+)/)?.[1] || '?'}`,
+              '',
+              '---',
+              '',
+              report,
+              '',
+              '<details><summary>Gate-Log</summary>',
+              '',
+              '```',
+              (log || '(kein Log)').slice(0, 20000),
+              '```',
+              '</details>',
+              '',
+              '---',
+              '_Automatisch erstellt/aktualisiert vom wöchentlichen SEO-Workflow (Bestand-Gate). ' +
+              'Pro Schadenslage gibt es genau EIN Issue – es wird bei Grün automatisch geschlossen._',
+            ].join('\n');
+
+            if (existing.length) {
+              const issue = existing[0];
+              await github.rest.issues.update({
+                owner: context.repo.owner, repo: context.repo.repo,
+                issue_number: issue.number, title, body,
+              });
+              await github.rest.issues.createComment({
+                owner: context.repo.owner, repo: context.repo.repo,
+                issue_number: issue.number,
+                body: `${marker}\n🔁 Bestand-Gate lief erneut (${new Date().toISOString()}), Befund aktualisiert. ` +
+                      `Exit-Code ${exitCode || '?'}.`,
+              });
+              console.log(`🔁 Issue #${issue.number} aktualisiert (kein Duplikat).`);
+              for (const dup of existing.slice(1)) {
+                await github.rest.issues.update({
+                  owner: context.repo.owner, repo: context.repo.repo,
+                  issue_number: dup.number, state: 'closed', state_reason: 'duplicate',
+                });
+                console.log(`🧹 Duplikat #${dup.number} geschlossen.`);
+              }
+            } else {
+              const created = await github.rest.issues.create({
+                owner: context.repo.owner, repo: context.repo.repo, title, body,
+              });
+              console.log(`✅ Issue #${created.data.number} erstellt.`);
+            }
+
+      - name: Bestand-Gate-Diagnose-Artefakt bei Rot
+        if: always() && steps.bestand.outputs.exit_code != '0'
+        uses: actions/upload-artifact@v4
+        with:
+          name: bestand-gate-run-${{ github.run_id }}
+          path: |
+            BESTAND-REPORT.md
+            .bestand_gate_state.json
+            /tmp/bestand_log.txt
+            /tmp/integrity_selftest.log
+          retention-days: 14
+          if-no-files-found: ignore
 
       - name: Cover-Validierung (alle Varianten – VOLLAUTOMATISCH)
         # Prüft für jeden Post, dass ALLE Cover-Varianten (620/720/avif/webp
