diff --git a/.github/workflows/seo-weekly.yml b/.github/workflows/seo-weekly.yml
index 6d6c9a2..9b38094 100644
--- a/.github/workflows/seo-weekly.yml
+++ b/.github/workflows/seo-weekly.yml
@@ -233,6 +233,12 @@ jobs:
           exit ${PIPESTATUS[0]}
 
       - name: Affiliate-Fixes committen
+        # SOFORTHEILUNGS-HÄRTUNG 02.09.2026: if: always() – auch wenn der
+        # Profi-Check wegen eines unheilbaren Rests Exit 1 meldet, werden
+        # bereits durchgeführte Heilungen IMMER committet (vorher gingen
+        # Teilheilungen beim Abbruch verloren; gleiche Absicherung wie beim
+        # Pinterest-SEO-Healer-Commit weiter oben).
+        if: always()
         run: |
           git config user.name "SEO-Bot"
           git config user.email "seo-bot@users.noreply.github.com"
