diff --git a/.github/workflows/affiliate-health.yml b/.github/workflows/affiliate-health.yml
index 3907787..791314b 100644
--- a/.github/workflows/affiliate-health.yml
+++ b/.github/workflows/affiliate-health.yml
@@ -56,6 +56,5 @@ jobs:
             echo "Kein Update."
           else
             git commit -m "chore+heal: Affiliate-Health E2E (Report${{ github.event_name == 'schedule' && ', Wochenwache' || '' }})"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push || { sleep 20; git pull --rebase origin main && git push; }
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
diff --git a/.github/workflows/affiliate-integrity-daily.yml b/.github/workflows/affiliate-integrity-daily.yml
index 4ea5794..832b74f 100644
--- a/.github/workflows/affiliate-integrity-daily.yml
+++ b/.github/workflows/affiliate-integrity-daily.yml
@@ -94,8 +94,7 @@ jobs:
             echo "Affiliate-Integritäts-Wache: keine Änderungen."
           else
             git commit -m "fix(content): Affiliate-Integritäts-Wache – defekte CTA-Box(en) automatisch geheilt (täglich)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "GEHEILT=true" >> "$GITHUB_ENV"
           fi
 
diff --git a/.github/workflows/alert-on-failure.yml b/.github/workflows/alert-on-failure.yml
index 35493c9..d5ba00c 100644
--- a/.github/workflows/alert-on-failure.yml
+++ b/.github/workflows/alert-on-failure.yml
@@ -22,7 +22,9 @@ on:
       - "Deploy-Catchup"                            # PREM-AUDIT: ohne ihn treibt ein Bot-Commit den Live-Stand nie nach
       - "Wöchentliche SEO-Optimierung"
       - "Backlink-Automation"
-      - "Wöchentliches Nach-Pinnen"
+      # AUDIT 27.08.: „Wöchentliches Nach-Pinnen“ entfernt – Workflow wurde
+      # stillgelegt (RSS-Auto-Publish von Pinterest übernimmt das Pinnen,
+      # manuelle Läufe laufen über „Pinterest-AI“ mit der neueren Engine).
       - "Quartalsweises Artikel-Update"
       - "Bot-Watchdog"
       - "Social-Media-AI"
diff --git a/.github/workflows/backlink-weekly.yml b/.github/workflows/backlink-weekly.yml
index 0fb4ded..afd5559 100644
--- a/.github/workflows/backlink-weekly.yml
+++ b/.github/workflows/backlink-weekly.yml
@@ -50,6 +50,5 @@ jobs:
             echo "Keine Änderungen – Report ist aktuell."
           else
             git commit -m "backlinks: wöchentlicher Scout-Report generiert"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
diff --git a/.github/workflows/blog-health-daily.yml b/.github/workflows/blog-health-daily.yml
index bd8e624..965a362 100644
--- a/.github/workflows/blog-health-daily.yml
+++ b/.github/workflows/blog-health-daily.yml
@@ -46,6 +46,5 @@ jobs:
             echo "Keine Änderungen – nichts zu pushen."
           else
             git commit -m "fix(auto): dauerhafte Blog-Gesundheit – Fehler geheilt"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
diff --git a/.github/workflows/bot-watchdog.yml b/.github/workflows/bot-watchdog.yml
index b2945d0..8ce4ac7 100644
--- a/.github/workflows/bot-watchdog.yml
+++ b/.github/workflows/bot-watchdog.yml
@@ -6,16 +6,26 @@
 #  Fehler), meldet sich sonst niemand – es käme einfach kein Artikel.
 #
 #  Der Watchdog prüft täglich:
-#    1) Wurde in den letzten ~30 h ein Artikel committet?
-#       (2 Posts/Tag erwartet → 30h-Fenster fängt Wochenend-/Feiertags-
-#        Ruhe ab und erkennt trotzdem einen kompletten Ausfall)
+#    1) KADENZ-BEWUSSTE Content-Erwartung (AUDIT-FIX 27.08.2026):
+#       scripts/publish_day_check.py liest die Artikel-Daten aus dem
+#       CONTENT (Frontmatter), nicht aus `git log` – und vergleicht mit
+#       dem erwarteten Publikationstag (Mo/Mi/Fr, Dauervorgabe).
+#       · Heute Publikationstag ohne Artikel → WARN (Fallback-Slots
+#         16:10/19:40 laufen noch – kein Issue, Engine-Phase 6 meldet
+#         sich abends selbst).
+#       · Letzter Mo/Mi/Fr OHNE Artikel → FAIL (echter Ausfall).
+#       (Der alte 30-h-Check meldete jeden SONNTAG fälschlich einen
+#       Ausfall – Fr→So-Lücke ~50 h, siehe Issue #57 – und hing an
+#       Commit-Messages, die nach der History-Konsolidierung vom
+#       27.08. nicht mehr existieren.)
 #    2) Sind alle Bot-Skripte syntaktisch valide?
-#    3) Ist die Live-Site auf dem neuesten Content-Commit (Deploy ok)?
+#    3) Ist die Live-Site auf dem neuesten Content (Deploy ok)?
 #    4) Ist das GitHub-TLS-Zertifikat für franksfinanzcheck.de ausgestellt?
 #       (Wenn ja → Hinweis-Issue: Cloudflare auf "Full (strict)" umstellen)
 #
 #  Bei Problemen: automatisch GitHub-Issue (mit Dedupe – kein Spam).
 #  Manuell starten: Actions → Bot-Watchdog → Run workflow
+
 # ============================================================
 name: Bot-Watchdog
 
@@ -43,22 +53,23 @@ jobs:
           PROBLEMS=""
           NOW=$(date +%s)
 
-          # 1) Letzter Content-Commit (letzte 30h)
-          LAST_TS=$(git log -1 --format=%ct --grep="^content:" || true)
-          if [ -z "$LAST_TS" ]; then
+          # 1) KADENZ-BEWUSSTE Content-Erwartung (AUDIT-FIX 27.08.2026):
+          #    liest Artikel-Daten aus dem Content selbst (robust gegen
+          #    History-Konsolidierung) und kennt die Mo/Mi/Fr-Dauervorgabe
+          #    inkl. Fallback-Slots. Exit: 0=OK · 3=WARN (kein Issue) · 1=FAIL.
+          set +e
+          PUBLISH_CHECK=$(python3 scripts/publish_day_check.py 2>&1)
+          PUBLISH_RC=$?
+          set -e
+          echo "Check 1: ${PUBLISH_CHECK}"
+          if [ "$PUBLISH_RC" -eq 0 ]; then
+            echo "CHECK1=OK" >> "$GITHUB_ENV"
+          elif [ "$PUBLISH_RC" -eq 3 ]; then
+            echo "CHECK1=WARN" >> "$GITHUB_ENV"
+          else
             PROBLEMS="$PROBLEMS
-          - **Kein Artikel seit Beginn** – noch nie ein Content-Commit"
+          - **${PUBLISH_CHECK}**"
             echo "CHECK1=FAIL" >> "$GITHUB_ENV"
-          else
-            AGE=$(( (NOW - LAST_TS) / 3600 ))
-            echo "Letzter Content-Commit vor ${AGE} h"
-            if [ "$AGE" -gt 30 ]; then
-              PROBLEMS="$PROBLEMS
-          - **Kein Artikel in den letzten ${AGE} h** (erwartet: 2/Tag) – Bot prüfen"
-              echo "CHECK1=FAIL" >> "$GITHUB_ENV"
-            else
-              echo "CHECK1=OK" >> "$GITHUB_ENV"
-            fi
           fi
 
           # 2) Skript-Syntax
@@ -71,26 +82,38 @@ jobs:
             echo "CHECK2=OK" >> "$GITHUB_ENV"
           fi
 
-          # 3) Live-Site aktuell? Der robusteste Check: den NEUESTEN Artikel aus
-          #    dem letzten Content-Commit nehmen und seine Live-URL per HTTP
-          #    prüfen (HTTP 200 = veröffentlicht).
-          LAST_SHA=$(git log -1 --format=%h --grep="^content:" || true)
-          if [ -n "$LAST_SHA" ]; then
-            NEWEST_SLUG=$(git show --stat --format="" "$LAST_SHA" | grep -o "content/posts/[^/]*" | sort -u | tail -1 | sed 's|content/posts/||')
-            if [ -n "$NEWEST_SLUG" ]; then
-              STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 20 "https://franksfinanzcheck.de/posts/${NEWEST_SLUG}/")
-              if [ "$STATUS" != "200" ]; then
-                PROBLEMS="$PROBLEMS
+          # 3) Live-Site aktuell? Der robusteste Check: den NEUESTEN LIVE-
+          #    Artikel direkt aus dem CONTENT (Frontmatter-datum, absteigend)
+          #    nehmen und seine Live-URL per HTTP prüfen (HTTP 200 =
+          #    veröffentlicht). AUDIT-FIX 27.08.: content-basiert statt
+          #    `git log --grep` – funktioniert auch nach History-
+          #    Konsolidierungen und an Di/Do/Sa/So.
+          NEWEST_SLUG=$(python3 - <<'PYEOF'
+          import glob, re
+          best, best_key = None, ("", "")
+          date_re = re.compile(r"^date:\s*[\"']?(\d{4}-\d{2}-\d{2})", re.M)
+          draft_re = re.compile(r"^draft:\s*true\b", re.M)
+          for f in glob.glob("content/posts/*/index.md"):
+              head = open(f, encoding="utf-8").read(2500)
+              if draft_re.search(head):
+                  continue  # Drafts sind nie live
+              m = date_re.search(head)
+              if m and (m.group(1), f) > best_key:
+                  best, best_key = f.split("/")[-2], (m.group(1), f)
+          print(best or "")
+          PYEOF
+          )
+          if [ -n "$NEWEST_SLUG" ]; then
+            STATUS=$(curl -s -o /dev/null -w "%{http_code}" --max-time 20 "https://franksfinanzcheck.de/posts/${NEWEST_SLUG}/")
+            if [ "$STATUS" != "200" ]; then
+              PROBLEMS="$PROBLEMS
           - **Neuester Artikel nicht live (HTTP ${STATUS})** – ${NEWEST_SLUG} (Deploy prüfen)"
-                echo "CHECK3=FAIL" >> "$GITHUB_ENV"
-              else
-                echo "CHECK3=OK (${NEWEST_SLUG} → 200)"
-              fi
+              echo "CHECK3=FAIL" >> "$GITHUB_ENV"
             else
-              echo "CHECK3=SKIP (kein Slug im Commit)"
+              echo "CHECK3=OK (${NEWEST_SLUG} → 200)"
             fi
           else
-            echo "CHECK3=SKIP (kein Content-Commit)"
+            echo "CHECK3=SKIP (kein Live-Artikel im Bestand)"
           fi
 
           # 4) GitHub-TLS-Zertifikat für franksfinanzcheck.de ausgestellt?
@@ -117,7 +140,7 @@ jobs:
         run: |
           echo "## 🤖 Watchdog-Ergebnis" >> "$GITHUB_STEP_SUMMARY"
           echo "" >> "$GITHUB_STEP_SUMMARY"
-          echo "- Check 1 (Artikel in 30h): ${{ env.CHECK1 }}" >> "$GITHUB_STEP_SUMMARY"
+          echo "- Check 1 (Kadenz: Artikel am erwarteten Mo/Mi/Fr-Tag): ${{ env.CHECK1 }}" >> "$GITHUB_STEP_SUMMARY"
           echo "- Check 2 (Skript-Syntax): ${{ env.CHECK2 }}" >> "$GITHUB_STEP_SUMMARY"
           echo "- Check 3 (Live-Site): ${{ env.CHECK3 }}" >> "$GITHUB_STEP_SUMMARY"
           echo "- Check 4 (GitHub-TLS-Zertifikat): ${{ env.CHECK4 }}" >> "$GITHUB_STEP_SUMMARY"
diff --git a/.github/workflows/content-engine-v2.yml b/.github/workflows/content-engine-v2.yml
index 7dc7c4c..12afa9c 100644
--- a/.github/workflows/content-engine-v2.yml
+++ b/.github/workflows/content-engine-v2.yml
@@ -99,8 +99,7 @@ jobs:
             echo "Kadenz-Gate: Bestand bereits konform."
           else
             git commit -m "fix(cadence): Kadenz-Selbstheilung vor Slot (Zurückstufung + Re-Queue)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh || echo "⚠ Push-Race erkannt – Heilung/Artikel bleibt im Runner, Folge-Phasen + Deploy-Gate pushen konvergent nach"  # AUDIT-FIX 27.08.
           fi
 
       # ================= PHASE 1: GENERIEREN (Multi-Fallback) =================
@@ -133,8 +132,7 @@ jobs:
           if git status --short content/ data/ | grep -q .; then
             git add content/ data/ static/images/covers/
             git commit -m "content: neuer Artikel automatisch generiert und veröffentlicht"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh || echo "⚠ Push-Race erkannt – Heilung/Artikel bleibt im Runner, Folge-Phasen + Deploy-Gate pushen konvergent nach"  # AUDIT-FIX 27.08.
             echo "✅ Phase-1-Commit: Artikel (oder Entwurf) veröffentlicht"
             echo "ARTIKEL_ERSTELLT=true" >> "$GITHUB_ENV"
           else
@@ -217,7 +215,7 @@ jobs:
           echo "== Oberarzt-Beweis nach der Geburt (Kette in Kanonfreiheit) =="
           python3 scripts/blog_doctor.py --new-only || echo "⚠ Doktor-Befund (nicht kritisch)"
           echo "== Qualitäts-Score (FrankAutoOps, Threshold >= 0.85) =="
-          python3 - <<'PYEOF'
+          python3 - <<'PYEOF' || echo "⚠ Qualitäts-Score (nicht kritisch)"  # AUDIT-FIX 27.08.: '|| echo' stand ALS EIGENE ZEILE nach dem Heredoc (Shell-Syntaxfehler) – Phase-2-Ende (Score, Cover, Kurzantworten, Fix-Commit) liefen NIE
           import glob, subprocess, sys, json, os
           import datetime
           sys.path.insert(0, "scripts")
@@ -250,7 +248,6 @@ jobs:
                   open(p, "w", encoding="utf-8").write(c.replace("draft: false", "draft: true", 1))
                   print(f"  → {r['slug']} auf draft gesetzt (Score {r['score']})")
           PYEOF
-          || echo "⚠ Qualitäts-Score (nicht kritisch)"
           echo "== Cover-Validierung (auto-nachziehen) =="
           python3 scripts/check_covers.py --fix || echo "⚠ Cover-Varianten (nicht blockierend)"
           echo "== Kurzantworten (Featured-Snippet-Box) =="
@@ -262,8 +259,7 @@ jobs:
             echo "Keine Qualitäts-Fixes."
           else
             git commit -m "content: Qualitäts-Fixes (Rechtschreibung, Grammatik, Cover, Polish)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Folge-Commit: Qualitäts-Fixes gepusht"
           fi
 
@@ -292,12 +288,42 @@ jobs:
             echo "Keine Optimierungs-Änderungen."
           else
             git commit -m "content: neuer Artikel sofort optimiert (Meta, interne Verlinkung)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Optimierungs-Commit gepusht"
           fi
 
       - name: Phase 3 – IndexNow-Ping
         if: ${{ !cancelled() && env.ARTIKEL_ERSTELLT == 'true' }}
         continue-on-error: true
-        run: python3 scripts/submit_indexnow.py || echo 
+        run: python3 scripts/submit_indexnow.py || echo
+
+      # ================= PHASE 4: STATUS, DASHBOARD & TAGESDEFIZIT (AUDIT-FIX 27.08.2026) =================
+      # Reaktivierung verwaister Wachen: engine_issue.py --deficit und
+      # bot_status.py waren in keinem Workflow mehr eingebunden (die alte
+      # Phase 6 ging in einem Refactor verloren), obwohl README/Report
+      # sie versprechen. Jetzt AGAIN am Tagesende jedes Engine-Laufs:
+      #   · BOT-STATUS.md  – Dashboard (veröffentlicht/Themenpool/Limits)
+      #   · --deficit      – Issue, wenn live < MIN_ARTIKEL_PRO_TAG,
+      #                      Auto-Close bei Wiederherstellung (Dedupe)
+      # Läuft bewusst IMMER (auch nach Teilfehlern) – genau dann braucht
+      # es die sichtbare Meldung.
+      - name: Phase 4 – Bot-Status + Tagesdefizit-Wache
+        if: ${{ !cancelled() }}
+        continue-on-error: true
+        env:
+          GITHUB_TOKEN: ${{ github.token }}
+          GITHUB_REPOSITORY: ${{ github.repository }}
+          MIN_ARTIKEL_PRO_TAG: ${{ vars.MIN_ARTIKEL_PRO_TAG || '2' }}
+          MAX_ARTIKEL_PRO_TAG: ${{ vars.MAX_ARTIKEL_PRO_TAG || '3' }}
+        run: |
+          python3 scripts/bot_status.py || echo "⚠ BOT-STATUS (nicht kritisch)"
+          python3 scripts/engine_issue.py --deficit || echo "⚠ Defizit-Wache (nicht kritisch)"
+          git config user.name "Content-Bot"
+          git config user.email "content-bot@users.noreply.github.com"
+          git add BOT-STATUS.md ENGINE-STATUS.md 2>/dev/null || true
+          if git diff --cached --quiet; then
+            echo "Keine Status-Änderungen."
+          else
+            git commit -m "chore(bot): Status-Dashboard aktualisiert [skip ci]"
+            bash scripts/git_push_retry.sh
+          fi
diff --git a/.github/workflows/deploy-catchup.yml b/.github/workflows/deploy-catchup.yml
index 5af7991..07cb24e 100644
--- a/.github/workflows/deploy-catchup.yml
+++ b/.github/workflows/deploy-catchup.yml
@@ -28,12 +28,19 @@ on:
   workflow_dispatch: {}
   workflow_run:
     workflows:
+      # AUDIT-FIX 27.08.2026: "Content-Engine v2" fehlte in dieser Liste –
+      # der eigene Deploy-Trigger-Schritt der Engine ging in einem früheren
+      # Refactor verloren. GITHUB_TOKEN-Pushes der Engine lösen deploy.yml
+      # NICHT aus → frisch publizierte Artikel (Mo/Mi/Fr 08:10) blieben
+      # bis zum nächsten fremden Push/Dienstläufer UNDEPLOYED. Über diesen
+      # zentralen, idempotenten Catchup (SHA-Vergleich) geht jeder
+      # Engine-Commit zuverlässig live.
+      - "Content-Engine v2"
       - "Wöchentliche SEO-Optimierung"
       - "Affiliate-Health (Wochenwache)"
       - "Backlink-Automation"
       - "Layout-AI"
       - "Quartalsweises Artikel-Update"
-      - "Wöchentliches Nach-Pinnen"
       - "Mastodon-Profil-Sync"
       - "Mastodon-Manueller-Post"
       - "Social-Media-AI"
diff --git a/.github/workflows/deploy.yml b/.github/workflows/deploy.yml
index 00daa6e..73d51cc 100644
--- a/.github/workflows/deploy.yml
+++ b/.github/workflows/deploy.yml
@@ -133,8 +133,7 @@ jobs:
             echo "Gate: keine Heilungen nötig."
           else
             git commit -m "fix(gate): Kadenz-/Titel-/Cover-Heilungen vor Veröffentlichung (automatisch)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Gate-Heilungen gepusht – Folge-Deploy prüft erneut (konvergent)."
           fi
 
diff --git a/.github/workflows/frankautoops-report.yml b/.github/workflows/frankautoops-report.yml
index 5a2d652..de75eef 100644
--- a/.github/workflows/frankautoops-report.yml
+++ b/.github/workflows/frankautoops-report.yml
@@ -56,8 +56,7 @@ jobs:
             echo "Report unverändert."
           else
             git commit -m "chore: FrankAutoOps-Report $(date +%F)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Report gepusht"
           fi
 
diff --git a/.github/workflows/layout-ai.yml b/.github/workflows/layout-ai.yml
index 3733e25..9e16688 100644
--- a/.github/workflows/layout-ai.yml
+++ b/.github/workflows/layout-ai.yml
@@ -70,8 +70,7 @@ jobs:
             echo "Keine Cover-Fixes nötig."
           else
             git commit -m "fix: Layout-Automatisierung – Cover-Varianten nachgezogen"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Cover-Fixes gepusht"
           fi
 
@@ -110,8 +109,7 @@ jobs:
             echo "Report unverändert."
           else
             git commit -m "chore: LAYOUT-REPORT aktualisiert"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       # ============ ISSUE BEI KRITISCHEN PROBLEMEN (Dedupe) ============
diff --git a/.github/workflows/link-check.yml b/.github/workflows/link-check.yml
index ebced49..3d22101 100644
--- a/.github/workflows/link-check.yml
+++ b/.github/workflows/link-check.yml
@@ -15,8 +15,11 @@
 name: Qualitäts-Gate (Build + interne Links)
 
 on:
-  push:
-    branches: [main, master]
+  # AUDIT-FIX 27.08.2026: push-Trigger entfernt. Der Deploy läuft bei JEDEM
+  # main-Push ohnehin durch dieselbe Gate-Kette (inkl. draft_link_healer +
+  # publish_gate) – der parallele Push-Build verdoppelte nur CI-Last und
+  # rote Runs (gestern 12× hintereinander), ohne eigenen Fund. Geblieben:
+  # nächtlicher Voll-Check + Pull-Requests + manuell.
   pull_request:
   schedule:
     - cron: "15 1 * * *"   # 03:15 MESZ – nachts (kein Push, nur Prüfung+Issue)
diff --git a/.github/workflows/mastodon-manual-post.yml b/.github/workflows/mastodon-manual-post.yml
index 71109ac..7d87e25 100644
--- a/.github/workflows/mastodon-manual-post.yml
+++ b/.github/workflows/mastodon-manual-post.yml
@@ -79,5 +79,4 @@ jobs:
           git config user.email "actions@users.noreply.github.com"
           git add data/social_log.jsonl
           git diff --cached --quiet || git commit -m "chore: manueller Mastodon-Post geloggt [skip ci]"
-          git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-          git push
\ No newline at end of file
+          bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
\ No newline at end of file
diff --git a/.github/workflows/mastodon-profile-sync.yml b/.github/workflows/mastodon-profile-sync.yml
index c306127..a797eda 100644
--- a/.github/workflows/mastodon-profile-sync.yml
+++ b/.github/workflows/mastodon-profile-sync.yml
@@ -57,5 +57,4 @@ jobs:
           git config user.email "actions@users.noreply.github.com"
           git add MASTODON-PROFILE-REPORT.md
           git diff --cached --quiet || git commit -m "chore: Mastodon-Profil-Sync-Report aktualisiert [skip ci]"
-          git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-          git push
\ No newline at end of file
+          bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
\ No newline at end of file
diff --git a/.github/workflows/mastodon-seo.yml b/.github/workflows/mastodon-seo.yml
index c88e227..08292e2 100644
--- a/.github/workflows/mastodon-seo.yml
+++ b/.github/workflows/mastodon-seo.yml
@@ -65,6 +65,5 @@ jobs:
             echo "Nichts zu committen."
           else
             git commit -m "chore: Mastodon-SEO-Report / Selbstheilung [skip ci]"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
diff --git a/.github/workflows/pinterest-ai.yml b/.github/workflows/pinterest-ai.yml
index 8d1a2a1..c649161 100644
--- a/.github/workflows/pinterest-ai.yml
+++ b/.github/workflows/pinterest-ai.yml
@@ -80,7 +80,6 @@ jobs:
             echo "Keine Änderungen."
           else
             git commit -m "chore: Pinterest-Status + Pins aktualisiert"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Pinterest-Status gepusht"
           fi
diff --git a/.github/workflows/pinterest-watchdog.yml b/.github/workflows/pinterest-watchdog.yml
index 4ca226f..a9f7193 100644
--- a/.github/workflows/pinterest-watchdog.yml
+++ b/.github/workflows/pinterest-watchdog.yml
@@ -86,8 +86,7 @@ jobs:
           git add -A
           if ! git diff --cached --quiet; then
             git commit -m "chore: Pinterest-Selbstheilung + Report"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Issue bei Problemen eröffnen
diff --git a/.github/workflows/repin-weekly.yml b/.github/workflows/repin-weekly.yml
deleted file mode 100644
index 987b485..0000000
--- a/.github/workflows/repin-weekly.yml
+++ /dev/null
@@ -1,73 +0,0 @@
-# ============================================================
-#  WÖCHENTLICHES AUTOMATISCHES NACH-PINNEN (Pinterest API)
-#
-#  Läuft jeden Montag um 17:30 Uhr (DE, Sommerzeit = 15:30 UTC).
-#  Warum Montag 17:30?
-#    - Wochenend-Artikel (Sa/So, 4 Stück) sind fertig
-#    - 1-2 Stunden VOR dem abendlichen Pinterest-Peak (19-22 Uhr),
-#      damit die Pins Zeit haben, in die Verteilung zu gehen
-#    - Kein Konflikt mit den Pin-Zeiten 08:00 und 19:30
-#
-#  Was passiert:
-#    - Findet alle Artikel mit "pinned: false"
-#    - Erstellt für jeden einen Pin (Cover-Bild, Meta-Description,
-#      Artikel-URL, Hashtags)
-#    - Setzt "pinned: true" und committet
-#
-#  VORAUSSETZUNG (einmalig, ~10 Min., siehe ANLEITUNG-PINTEREST-API.md):
-#    - Pinterest-Developer-App + Access-Token
-#      → Secret: PINTEREST_ACCESS_TOKEN
-#    - Board-ID (mit --list-boards ermitteln)
-#      → Variable: PINTEREST_BOARD_ID
-#
-#  Manuell starten: GitHub → Actions → "Wöchentliches Nach-Pinnen"
-#  → "Run workflow".
-# ============================================================
-name: Wöchentliches Nach-Pinnen
-
-on:
-  # 20.08.2026: Automatisches Pinnen übernimmt jetzt Pinterests
-  # RSS-Auto-publish (Feed: https://franksfinanzcheck.de/index.xml).
-  # Dieser Workflow bleibt nur für MANUELLE Läufe erhalten –
-  # kein automatisches Pinnen mehr (verhindert Doppel-Pins).
-  workflow_dispatch: {}
-
-permissions:
-  contents: write
-
-concurrency:          # AUDIT-FIX: gegen Doppelläufe
-  group: content-pins
-  cancel-in-progress: false
-
-jobs:
-  nachpinnen:
-    runs-on: ubuntu-latest
-    steps:
-      - name: Repository auschecken
-        uses: actions/checkout@v4
-
-      - name: Krypto- und YAML-Bibliotheken für Token-Auto-Refresh & Pins installieren
-        run: pip install -q pyyaml cryptography
-
-      - name: Artikel bei Pinterest nachpinnen
-        env:
-          PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
-          PINTEREST_TOKEN_KEY: ${{ secrets.PINTEREST_TOKEN_KEY }}
-          PINTEREST_BOARD_ID: ${{ vars.PINTEREST_BOARD_ID }}
-          BLOG_BASE_URL: "https://franksfinanzcheck.de"
-        run: python3 scripts/generate_pins.py
-
-      - name: pinned-Flags committen & pushen
-        if: ${{ !cancelled() }}
-        run: |
-          git config user.name "Content-Bot"
-          git config user.email "content-bot@users.noreply.github.com"
-          git add -A
-          if git diff --cached --quiet; then
-            echo "Keine Änderungen – nichts zu committen."
-          else
-            git commit -m "content: Artikel als gepinnt markiert (Pinterest)"
-            # AUDIT-FIX: Rebase-Schutz gegen Push-Race mit anderen Bots
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push || { sleep 20; git pull --rebase origin main && git push; }
-          fi
diff --git a/.github/workflows/seo-weekly.yml b/.github/workflows/seo-weekly.yml
index dcbd9e4..7db0770 100644
--- a/.github/workflows/seo-weekly.yml
+++ b/.github/workflows/seo-weekly.yml
@@ -73,8 +73,7 @@ jobs:
             echo "Keine Keyword-Änderungen"
           else
             git commit -m "seo: LSI-Keywords für neue Artikel ergänzt (KI-Vorschläge)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Interne Verlinkung automatisch ausbauen
@@ -95,8 +94,7 @@ jobs:
             echo "Keine neuen internen Links (Struktur bereits dicht)."
           else
             git commit -m "seo: interne Verlinkung ausgebaut (kontextuelle Links, nur Live-Ziele)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Meta-Daten-Optimierung (Title/Description/Keywords)
@@ -130,8 +128,7 @@ jobs:
             echo "Keine Pinterest/SEO-Heiler-Änderungen"
           else
             git commit -m "seo: Pinterest/Google-SEO-Healer (Titel, Cover, Pin-Texte)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
       - name: Meta-Änderungen committen
         if: steps.meta.outcome == 'success'
@@ -143,8 +140,7 @@ jobs:
             echo "Keine Meta-Änderungen"
           else
             git commit -m "seo: Meta-Daten optimiert (Title/Description/Keywords)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Rechtschreib- & Groß-Klein-Prüfung aller Artikel (Top-Level)
@@ -213,8 +209,7 @@ jobs:
             echo "Keine Affiliate-Änderungen."
           else
             git commit -m "seo: Affiliate-Deep-Links korrigiert (vollautomatisch)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Affiliate-Fixes gepusht"
           fi
 
@@ -257,8 +252,7 @@ jobs:
             echo "Bestand-Gate: keine Änderungen."
           else
             git commit -m "seo: Bestand-Gate – bestehende Artikel gegen aktuelle Publish-Regeln geheilt"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Issue bei nicht automatisch heilbaren Bestandsartikeln
@@ -297,8 +291,7 @@ jobs:
             echo "Keine Cover-Änderungen."
           else
             git commit -m "seo: Cover-Varianten nachgezogen (vollautomatisch)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Cover-Fixes gepusht"
           fi
 
@@ -332,8 +325,7 @@ jobs:
           if git diff --cached --quiet; then echo "Textbatterie: sauber."
           else
             git commit -m "chore: Textbatterie nach SEO-Edits (selbstheilend)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push || { sleep 20; git pull --rebase origin main && git push; }
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: lastmod für geänderte Artikel setzen (Frische-Signal)
@@ -359,8 +351,7 @@ jobs:
             echo "Report unverändert"
           else
             git commit -m "seo: Meta-Report aktualisiert (vollautomatisch)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Issue bei verbleibenden Meta-Problemen (kein manuelles Sichten nötig)
@@ -430,8 +421,7 @@ jobs:
             echo "Keine Änderungen"
           else
             git commit -m "seo: Bing-Indexierungs-Status aktualisiert"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Issue bei kritischen SEO-Problemen erstellen
diff --git a/.github/workflows/social-ai.yml b/.github/workflows/social-ai.yml
index 7b36655..a40001a 100644
--- a/.github/workflows/social-ai.yml
+++ b/.github/workflows/social-ai.yml
@@ -59,7 +59,6 @@ jobs:
             echo "Nichts zu committen."
           else
             git commit -m "social: neue Artikel auf Social Media geteilt"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "✅ Social-Update gepusht"
           fi
diff --git a/.github/workflows/update-quarterly.yml b/.github/workflows/update-quarterly.yml
index a932501..fd8e8e6 100644
--- a/.github/workflows/update-quarterly.yml
+++ b/.github/workflows/update-quarterly.yml
@@ -24,10 +24,12 @@ name: Quartalsweises Artikel-Update
 
 on:
   schedule:
-    - cron: "0 6 1 1,4,7,10 *"    # 1. Jan/Apr/Jul/Okt 07:00 MESZ (06:00 UTC)
-    # Winterzeit-Fallback: 1 Stunde später in UTC, damit es weiterhin
-    # ~07:00 MEZ ist:
-    - cron: "0 7 1 1,4,7,10 *"
+    # AUDIT-FIX 27.08.2026: Der frühere „Winterzeit-Fallback“-Zweitcron
+    # (0 7 1 1,4,7,10 *) lief an JEDEM Quartalstag ZWEIMAL (GitHub Cron
+    # kennt keine Sommer-/Winterzeit – beide Einträge feuern immer).
+    # Folge: doppelter KI-Aufwand + doppelter Push-Race. Jetzt: EIN Cron,
+    # die ±1 h Jahresszeit-Verschiebung ist bei einem Quartalsjob egal.
+    - cron: "0 6 1 1,4,7,10 *"    # 1. Jan/Apr/Jul/Okt ~07:00 MEZ/MESZ
   workflow_dispatch: {}
 
 permissions:
@@ -80,8 +82,7 @@ jobs:
             echo "Keine Artikel aktualisiert."
           else
             git commit -m "content: Quartalsweises Artikel-Update – Top-Artikel aufgefrischt (Frische-Signal)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push  # AUDIT-FIX: Rebase-Schutz
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
           fi
 
       - name: Aktualisierte Artikel bei IndexNow einreichen
diff --git a/.github/workflows/willkommenstext-refresh.yml b/.github/workflows/willkommenstext-refresh.yml
index 7886e23..e3f07ad 100644
--- a/.github/workflows/willkommenstext-refresh.yml
+++ b/.github/workflows/willkommenstext-refresh.yml
@@ -36,6 +36,9 @@ on:
   schedule:
     - cron: "0 6 * * 0"    # wöchentlich Sonntag 08:00 MESZ (06:00 UTC)
   push:
+    # AUDIT-FIX 27.08.: nur main – sonst liefe der Refresh auch auf jedem
+    # Feature-Branch, der diese Pfade touchiert.
+    branches: [main]
     paths:
       - "data/aktuelle_entwicklungen.yaml"
       - "SEO-STANDARDS-2026.md"
@@ -114,8 +117,7 @@ jobs:
             echo "Willkommenstext-Refresh: keine Änderungen."
           else
             git commit -m "content: Willkommenstext automatisch aufgefrischt (SEO-Unique-Content-Automatik)"
-            git pull --rebase --autostash origin main 2>/dev/null || git rebase --abort 2>/dev/null || true
-            git push
+            bash scripts/git_push_retry.sh  # AUDIT-FIX 27.08.: kollisionssicher (3× Retry, nie still, nie force)
             echo "AKTUALISIERT=true" >> "$GITHUB_ENV"
           fi
 
