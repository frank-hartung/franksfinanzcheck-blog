diff --git a/.github/workflows/pinterest-ai.yml b/.github/workflows/pinterest-ai.yml
index 58c6cec..dab026e 100644
--- a/.github/workflows/pinterest-ai.yml
+++ b/.github/workflows/pinterest-ai.yml
@@ -56,6 +56,17 @@ jobs:
         run: |
           echo "bereit=true" >> "$GITHUB_OUTPUT"
 
+      # ZIEL-LINK- UND REPEAT-PIN-GATE (02.09.2026, nach der Pinterest-Sperre):
+      # P8 prueft, dass jeder Pin eine konkrete eigene Artikel-URL verlinkt
+      # (nie das Pinterest-Profil, nie die nackte Startseite, immer https) -
+      # das war der Sperrgrund vom 15.08. P6/P7 begrenzen zusaetzlich auf
+      # max. 3 Pins je Ziel und >=7 Tage Abstand. P8-Befunde stoppen den Lauf
+      # hart, weil kein Skript erraten kann, welcher Artikel gemeint war.
+      - name: Ziel-Link- und Repeat-Pin-Gate (Plan-Guard P1-P8)
+        run: |
+          python3 scripts/pinterest_plan_guard.py --selftest
+          python3 scripts/pinterest_plan_guard.py --fix
+
       - name: Pinterest-Engine ausführen (Posten oder Queue)
         if: steps.bereit.outputs.bereit == 'true'
         env:
@@ -74,6 +85,7 @@ jobs:
           # pinterest_tokens.enc ist erst da, wenn du den OAuth-Handschlag
           # gemacht hast – ohne Datei trotzdem committen (12.08. gehaertet).
           git add PIN-STATUS.md data/pin_queue.yaml content/posts/ 2>/dev/null || true
+          git add data/pinterest_plan.yaml data/pinterest_plan_parked.yaml PINTEREST-PLAN-GUARD-REPORT.md 2>/dev/null || true
           [ -f data/pinterest_tokens.enc ] && git add data/pinterest_tokens.enc 2>/dev/null || true
           [ -f data/pinterest_boards_cache.json ] && git add data/pinterest_boards_cache.json 2>/dev/null || true
           if git diff --cached --quiet; then
diff --git a/.github/workflows/repin-weekly.yml b/.github/workflows/repin-weekly.yml
index f1cebaa..d734d0e 100644
--- a/.github/workflows/repin-weekly.yml
+++ b/.github/workflows/repin-weekly.yml
@@ -49,6 +49,17 @@ jobs:
       - name: Krypto- und YAML-Bibliotheken für Token-Auto-Refresh & Pins installieren
         run: pip install -q pyyaml cryptography
 
+      # ZIEL-LINK- UND REPEAT-PIN-GATE (02.09.2026, nach der Pinterest-Sperre):
+      # P8 prueft, dass jeder Pin eine konkrete eigene Artikel-URL verlinkt
+      # (nie das Pinterest-Profil, nie die nackte Startseite, immer https) -
+      # das war der Sperrgrund vom 15.08. P6/P7 begrenzen zusaetzlich auf
+      # max. 3 Pins je Ziel und >=7 Tage Abstand. P8-Befunde stoppen den Lauf
+      # hart, weil kein Skript erraten kann, welcher Artikel gemeint war.
+      - name: Ziel-Link- und Repeat-Pin-Gate (Plan-Guard P1-P8)
+        run: |
+          python3 scripts/pinterest_plan_guard.py --selftest
+          python3 scripts/pinterest_plan_guard.py --fix
+
       - name: Artikel bei Pinterest nachpinnen
         env:
           PINTEREST_ACCESS_TOKEN: ${{ secrets.PINTEREST_ACCESS_TOKEN }}
